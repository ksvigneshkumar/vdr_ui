export function hasPermission(role, permissionKey) {
  // Mock function for frontend demo since backend was removed.
  // Returns true for all permissions to allow full UI exploration.
  return true;
}
